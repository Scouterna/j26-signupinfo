[project]
name = "j26-signupinfo"
version = "0.4.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "httpx[http2]",
    "joserfc",
    "itsdangerous",
    "async-lru",
    "prometheus-fastapi-instrumentator",
]
